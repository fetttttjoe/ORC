{
  "name": "orchestrator",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "plugins/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p packages/contracts && tsc --noEmit -p packages/kernel && tsc --noEmit -p packages/cli && tsc --noEmit -p plugins/provider-anthropic && tsc --noEmit -p plugins/provider-openai && tsc --noEmit -p plugins/provider-ollama",
    "db:up": "docker compose up -d --wait"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "drizzle-kit": "^0.31.10",
    "typescript": "^7.0.2"
  }
}
