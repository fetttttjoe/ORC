{
  "name": "orchestrator",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "plugins/*"],
  "scripts": {
    "orc": "bun packages/cli/src/bin.ts",
    "graph": "bun packages/cli/src/bin.ts graph",
    "e2e": "cd packages/graph-ui && bunx playwright test",
    "test": "bun test",
    "test:live": "bun test plugins/executor-api-loop/src/live-smoke.test.ts",
    "typecheck": "tsc --noEmit -p packages/contracts && tsc --noEmit -p packages/kernel && tsc --noEmit -p packages/cli && tsc --noEmit -p plugins/provider-anthropic && tsc --noEmit -p plugins/provider-openai && tsc --noEmit -p plugins/provider-ollama && tsc --noEmit -p plugins/executor-api-loop && tsc --noEmit -p plugins/mcp-client && tsc --noEmit -p packages/vault-projector && tsc --noEmit -p plugins/memory && tsc --noEmit -p plugins/analyzer-agent && tsc --noEmit -p packages/ui-core && tsc --noEmit -p packages/graph-ui",
    "db:up": "docker compose up -d --wait"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "patchedDependencies": {
    "surqlize@0.1.0": "patches/surqlize@0.1.0.patch"
  }
}
